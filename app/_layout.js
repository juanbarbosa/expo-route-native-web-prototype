/*
import { Slot } from 'expo-router';
import { VStack, GluestackUIProvider, Heading } from '@gluestack-ui/themed';

export default function RootLayout() {
  return (
    <GluestackUIProvider>
      <Heading>Root Header?</Heading>
      <Slot />
    </GluestackUIProvider>
  );
}
*/